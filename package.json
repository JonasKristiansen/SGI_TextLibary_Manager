{
  "name": "sgi-text-library-manager",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "dev": "NODE_ENV=development nodemon server.js",
    "build": "cd ui && npm install && npm run build && cd .. && rm -rf static && cp -r ui/dist static"
  },
  "dependencies": {
    "axios": "^1.7.2",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "pg": "^8.11.0",
    "pg-pool": "^3.6.1"
  },
  "devDependencies": {
    "nodemon": "^3.1.4"
  }
}
